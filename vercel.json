{
    "buildCommand": "npm install --prefix frontend && npm run build --prefix frontend",
    "outputDirectory": "frontend/dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "https://rag-chatbot-cocl.onrender.com/api/:path*"
        },
        {
            "source": "/auth/:path*",
            "destination": "https://rag-chatbot-cocl.onrender.com/auth/:path*"
        }
    ]
}